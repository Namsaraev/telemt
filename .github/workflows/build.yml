name: Build

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.1

      - name: Install latest stable Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry & build artifacts
        uses: actions/cache@v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Test WEB bridge HTTP methods
        run: node --test src/web/bridge/request.test.cjs

      - name: Test Nginx CDN method restoration
        run: |
          sudo apt-get update
          sudo apt-get install -y nginx
          node --test src/web/bridge/nginx.test.cjs

      - name: Test WEB configuration and protocol
        run: cargo test web

      - name: Build Release
        run: cargo build --release --verbose

      - name: Upload binary artifact
        uses: actions/upload-artifact@v7.0.1
        with:
          name: telemt
          path: target/release/telemt
